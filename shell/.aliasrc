alias clean_mail='dd if=/dev/null of=/var/mail/wolfo'
function exsc {
    exsi -t nasm-linux-x86_64 $1 && `echo "./$1" | sed 's/\.xes$//g'`
}

unalias ls
unalias ll
unalias la
unalias l

alias ls='exa --icons'
alias ll='ls -lh'
alias la='ll -A --git-ignore'
alias l='ll -a'
# vim:ft=bash:
